#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

int maximumLength(int* nums, int numsSize, int k) {
    int i, j;
    int ret;
    
    return 0;
}

int main(){
    int i,j;
    int out;
    int numsSize=5;
    int nums[5]={1, 2, 1, 1, 3};
    int k=2;
    out=maximumLength(nums, numsSize, k);
    printf("__out = %d__\n", out);
    return 0;
}
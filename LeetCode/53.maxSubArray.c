#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

int maxSubArray(int* nums, int numsSize) {
    int i, j;
    int out=0;// 记录返回值
    


    return out;
}

int main(){
    int i, j;
    int nums[]={-2, 1, -2, 4, -1, 2, 1, -5, 4};
    int numsSize=9;
    int out=0;

    out=maxSubArray(nums, numsSize);
    printf("out = %d\n", out);
    return 0;
}